{
  "name": "@luxgen/mcp-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "tsc && node dist/index.js",
    "start": "node dist/index.js",
    "build": "tsc",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@luxgen/mcp-core": "*",
    "dotenv": "^16.3.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  }
}
