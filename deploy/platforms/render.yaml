# LuxGen — Render Blueprint (API + agent worker)
# Docs: docs/deployment/FREE_TIER_CLOUD.md · deploy/README.md
# Usage: Render Dashboard → New Blueprint → connect repo

services:
  - type: web
    name: luxgen-api
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./apps/api/Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: '4000'
      - key: APOLLO_INTROSPECTION
        value: 'false'
      - key: BILLING_DEV_MODE
        value: 'false'
      - key: LOG_LEVEL
        value: info
      - key: EMAIL_PROVIDER
        value: log
      # Set these in Render dashboard (secrets):
      - key: MONGODB_URI
        sync: false
      - key: REDIS_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: CORS_ORIGIN
        sync: false
      - key: WEB_APP_URL
        sync: false
      - key: STRIPE_SECRET_KEY
        sync: false
      - key: STRIPE_WEBHOOK_SECRET
        sync: false
      - key: STRIPE_PRICE_STARTER
        sync: false
      - key: STRIPE_PRICE_PRO
        sync: false
      - key: STRIPE_PRICE_BUSINESS
        sync: false
      - key: STRIPE_PRICE_LISTING
        sync: false
      - key: JOBS_API_KEY
        generateValue: true

  - type: web
    name: luxgen-mcp
    runtime: docker
    plan: free
    region: oregon
    dockerfilePath: ./apps/mcp-server/Dockerfile
    dockerContext: .
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: MCP_TRANSPORT
        value: http
      - key: MCP_HTTP_PORT
        value: '3100'
      - key: MCP_HTTP_HOST
        value: 0.0.0.0
      - key: MCP_RATE_LIMIT_MAX
        value: '120'
      - key: MCP_RATE_LIMIT_WINDOW_MS
        value: '60000'
      # Set in Render dashboard — e.g. https://luxgen-api.onrender.com/graphql
      - key: LUXGEN_GRAPHQL_URL
        sync: false
      # Comma-separated hostnames for MCP DNS rebinding protection (your Render MCP URL host)
      - key: MCP_ALLOWED_HOSTS
        sync: false

# Optional: deploy web on Render instead of Vercel (uses free tier hours)
#  - type: web
#    name: luxgen-web
#    runtime: docker
#    plan: free
#    dockerfilePath: ./apps/web/Dockerfile
#    dockerContext: .
#    healthCheckPath: /api/health
#    envVars:
#      - key: NODE_ENV
#        value: production
#      - key: API_URL
#        fromService:
#          name: luxgen-api
#          type: web
#          property: hostport
#      - key: NEXT_PUBLIC_GRAPHQL_URL
#        sync: false
#      - key: NEXT_PUBLIC_APP_URL
#        sync: false
