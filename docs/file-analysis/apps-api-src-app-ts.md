# app.ts (API) — Brief + Junior Q&A

**Path:** `apps/api/src/app.ts`  
**Role:** Express app setup — middleware, routes, Apollo Server, WebSocket.

---

## Junior Q&A

--------------------------------------------------------------------------------------------------------------------------------------------
**[0] What framework is the API built with?**
--------------------------------------------------------------------------------------------------------------------------------------------

**ANS.** Express + Apollo Server GraphQL — imports `app.ts:5-6`.

--------------------------------------------------------------------------------------------------------------------------------------------
**[1] Name two security middlewares at the top.**
--------------------------------------------------------------------------------------------------------------------------------------------

**ANS.** `helmet()` and `cors()` — `app.ts:43-50`.

--------------------------------------------------------------------------------------------------------------------------------------------
**[2] What middleware sets `req.tenantId`?**
--------------------------------------------------------------------------------------------------------------------------------------------

**ANS.** `tenantRoutingMiddleware` from `middleware/tenantRouting.ts` — imported `app.ts:19`.

--------------------------------------------------------------------------------------------------------------------------------------------
**[3] What middleware sets `req.user`?**
--------------------------------------------------------------------------------------------------------------------------------------------

**ANS.** `authMiddleware` — imported `app.ts:17`.

--------------------------------------------------------------------------------------------------------------------------------------------
**[4] Where is GraphQL mounted?**
--------------------------------------------------------------------------------------------------------------------------------------------

**ANS.** Apollo Server applies middleware to Express; context from `context()` — see `app.ts` GraphQL setup section.

--------------------------------------------------------------------------------------------------------------------------------------------
**[5] REST routes examples?**
--------------------------------------------------------------------------------------------------------------------------------------------

**ANS.** `auth`, `billing`, `tenant` — imports `app.ts:28-36`.

---

**More:** [15-junior-qa-mern.md](../interview-prep/15-junior-qa-mern.md#2-why-does-middleware-order-matter)
