{
  "name": "@luxgen/presenters",
  "version": "1.0.0",
  "private": true,
  "description": "Shared data-fetching + view-model presenter layer (queries -> fetchers -> transformers -> hook), consumed by both apps/web and apps/mobile. See README.md and docs/CROSS_PLATFORM_RESTRUCTURE.md.",
  "main": "index.ts",
  "types": "index.ts",
  "scripts": {
    "build": "tsc --noEmit",
    "lint": "tsc --noEmit",
    "test:auto": "vitest run"
  },
  "peerDependencies": {
    "@apollo/client": "*",
    "graphql": "*",
    "react": ">=18.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "typescript": "^5.0.0",
    "vitest": "^2.1.9"
  }
}
