{
  "name": "@luxgen/types",
  "version": "1.0.0",
  "private": true,
  "description": "Shared TypeScript interfaces for LuxGen web and mobile (no runtime deps)",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@luxgen/db": "*"
  },
  "devDependencies": {
    "typescript": "^5.0.0"
  }
}
