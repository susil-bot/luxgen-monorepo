import type { CountryLanguageDropdownProps } from './CountryLanguageDropdown';

import { defaultTheme } from '../theme';

export const countryLanguageDropdownFixtures = {
  default: {
    tenantTheme: defaultTheme,
  } as CountryLanguageDropdownProps,
};
