import { css } from '@emotion/css';

export const countryLanguageDropdownStyles = {
  root: css`
    .country-language-dropdown {
      font-family: var(--font-primary);
      color: var(--color-text);
    }
  `,
};
