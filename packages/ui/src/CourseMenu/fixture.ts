import type { CourseMenuProps } from './CourseMenu';

import { defaultTheme } from '../theme';

export const courseMenuFixtures = {
  default: {
    tenantTheme: defaultTheme,
  } as CourseMenuProps,
};
