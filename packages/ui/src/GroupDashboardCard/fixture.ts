import type { GroupDashboardCardProps } from './GroupDashboardCard';

import { defaultTheme } from '../theme';

export const groupDashboardCardFixtures = {
  default: {
    tenantTheme: defaultTheme,
  } as GroupDashboardCardProps,
};
