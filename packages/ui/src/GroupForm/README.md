# GroupForm

LuxGen UI component. See `GroupForm.tsx` for props and usage.

## Structure

| File                | Purpose                         |
| ------------------- | ------------------------------- |
| `GroupForm.tsx`     | Main component                  |
| `fetcher.ts`        | Data fetching / SSR helpers     |
| `fixture.ts`        | Test fixtures and mock data     |
| `GroupForm.spec.ts` | Unit tests                      |
| `styles.ts`         | Styled components and CSS-in-JS |
| `translations.ts`   | i18n strings                    |
| `index.ts`          | Public exports                  |

## Usage

```tsx
import { GroupForm } from '@luxgen/ui';

<GroupForm />;
```
