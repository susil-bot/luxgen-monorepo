import type { GroupMemberListProps } from './GroupMemberList';

import { defaultTheme } from '../theme';

export const groupMemberListFixtures = {
  default: {
    tenantTheme: defaultTheme,
  } as GroupMemberListProps,
};
