import { css } from '@emotion/css';

export const groupMemberListStyles = {
  root: css`
    .group-member-list {
      font-family: var(--font-primary);
      color: var(--color-text);
    }
  `,
};
