import type { LastSurveyProps } from './LastSurvey';

import { defaultTheme } from '../theme';

export const lastSurveyFixtures = {
  default: {
    tenantTheme: defaultTheme,
  } as LastSurveyProps,
};
