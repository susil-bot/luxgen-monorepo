import type { RegisterFormProps } from './RegisterForm';

import { defaultTheme } from '../theme';

export const registerFormFixtures = {
  default: {
    tenantTheme: defaultTheme,
  } as RegisterFormProps,
};
