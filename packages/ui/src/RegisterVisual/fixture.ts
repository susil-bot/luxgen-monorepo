import type { RegisterVisualProps } from './RegisterVisual';

import { defaultTheme } from '../theme';

export const registerVisualFixtures = {
  default: {
    tenantTheme: defaultTheme,
  } as RegisterVisualProps,
};
