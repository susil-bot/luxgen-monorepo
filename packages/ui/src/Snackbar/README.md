# Snackbar

LuxGen UI component. See `Snackbar.tsx` for props and usage.

## Structure

| File               | Purpose                         |
| ------------------ | ------------------------------- |
| `Snackbar.tsx`     | Main component                  |
| `fetcher.ts`       | Data fetching / SSR helpers     |
| `fixture.ts`       | Test fixtures and mock data     |
| `Snackbar.spec.ts` | Unit tests                      |
| `styles.ts`        | Styled components and CSS-in-JS |
| `translations.ts`  | i18n strings                    |
| `index.ts`         | Public exports                  |

## Usage

```tsx
import { Snackbar } from '@luxgen/ui';

<Snackbar />;
```
