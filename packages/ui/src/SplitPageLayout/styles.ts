import { css } from '@emotion/css';

export const splitPageLayoutStyles = {
  root: css`
    .split-page-layout {
      font-family: var(--font-primary);
      color: var(--color-text);
    }
  `,
};
