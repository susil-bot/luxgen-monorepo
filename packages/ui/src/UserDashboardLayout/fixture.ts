import type { UserDashboardLayoutProps } from './UserDashboardLayout';

import { defaultTheme } from '../theme';

export const userDashboardLayoutFixtures = {
  default: {
    tenantTheme: defaultTheme,
  } as UserDashboardLayoutProps,
};
