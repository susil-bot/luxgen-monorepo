import type { UserTableProps } from './UserManagement';

import { defaultTheme } from '../theme';

export const userManagementFixtures = {
  default: {
    tenantTheme: defaultTheme,
  } as UserTableProps,
};
