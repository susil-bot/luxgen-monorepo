import type { UserRetentionProps } from './UserRetention';

import { defaultTheme } from '../theme';

export const userRetentionFixtures = {
  default: {
    tenantTheme: defaultTheme,
  } as UserRetentionProps,
};
